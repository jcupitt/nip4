// shims for watch.h, until we get it in

#define NIP4_PATH "$HOME/.nip4-0.1.0"
#define PATH_TMP (NIP4_PATH "/tmp")
#define PATH_SEARCH (path_parse(NIP4_PATH "/start"))
#define AUTO_WS_SAVE (TRUE)
#define PRINT_CARTESIAN (FALSE)
#define TRACE_FUNCTIONS (FALSE)
#define LINELENGTH (80)
#define MAX_HEAPSIZE (10000000)
#define WORKSPACE_LPANE_OPEN (FALSE)
#define WORKSPACE_LPANE_POSITION (200)
#define WORKSPACE_RPANE_OPEN (FALSE)
#define WORKSPACE_RPANE_POSITION (400)
